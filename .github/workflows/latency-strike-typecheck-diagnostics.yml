name: Latency Strike Typecheck Diagnostics

on:
  push:
    branches:
      - feature/latency-strike-v1
    paths:
      - .github/workflows/latency-strike-typecheck-diagnostics.yml

permissions:
  contents: read
  pull-requests: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/latency-strike-v1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: worker/package-lock.json
      - run: cd worker && npm ci
      - name: Capture TypeScript output
        shell: bash
        run: |
          cd worker
          set +e
          npm run typecheck > latency-typecheck.txt 2>&1
          echo $? > latency-typecheck-exit.txt
          set -e
      - name: Publish diagnostics
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('node:fs');
            const report = fs.readFileSync('worker/latency-typecheck.txt', 'utf8').slice(-30000);
            const exitCode = fs.readFileSync('worker/latency-typecheck-exit.txt', 'utf8').trim();
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: 23,
              body: `## Latency Strike TypeScript diagnostics\n\nExit: \`${exitCode}\`\n\n\`\`\`text\n${report}\n\`\`\``
            });
