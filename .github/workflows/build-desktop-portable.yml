name: Build Desktop Portable

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - desktop_launcher/**
      - .github/workflows/build-desktop-portable.yml
  pull_request:
    paths:
      - desktop_launcher/**
      - .github/workflows/build-desktop-portable.yml

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
        working-directory: desktop_launcher
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Validate external engine GUI
        run: |
          python -m py_compile .\external_engines\spotify_oggmp4_gui.py
          python -m unittest external_engines.test_spotify_oggmp4_gui

      - name: Build Windows EXEs
        run: |
          pyinstaller --noconfirm --clean --onefile --noconsole --name SoundDropDesktop sounddrop_desktop.py
          pyinstaller --noconfirm --clean --onefile --noconsole --name DyrakArmySpotifyOggMp4Engine .\external_engines\spotify_oggmp4_gui.py

      - name: Upload Windows artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: SoundDropDesktop-windows
          path: |
            desktop_launcher/dist/SoundDropDesktop.exe
            desktop_launcher/dist/DyrakArmySpotifyOggMp4Engine.exe

  build-macos:
    runs-on: macos-latest
    defaults:
      run:
        shell: bash
        working-directory: desktop_launcher
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyobjc-framework-Cocoa

      - name: Validate external engine GUI
        run: |
          python -m py_compile ./external_engines/spotify_oggmp4_gui.py
          python -m unittest external_engines.test_spotify_oggmp4_gui

      - name: Build macOS app bundle
        run: |
          pyinstaller --noconfirm --clean --windowed --name SoundDropDesktop sounddrop_desktop.py

      - name: Archive macOS app
        run: |
          cd dist
          ditto -c -k --sequesterRsrc --keepParent "SoundDropDesktop.app" "SoundDropDesktop-macOS.zip"

      - name: Upload macOS artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: SoundDropDesktop-macos
          path: desktop_launcher/dist/SoundDropDesktop-macOS.zip
