name: Unified TypeScript Diagnostics

on:
  push:
    branches:
      - feature/dyrakarmy-arena-v1
    paths:
      - .github/workflows/unified-typecheck-diagnostics.yml
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/dyrakarmy-arena-v1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: worker/package-lock.json
      - name: Capture TypeScript output
        shell: bash
        run: |
          cd worker
          npm ci
          set +e
          npm run typecheck > typecheck.txt 2>&1
          echo $? > typecheck-exit.txt
          set -e
      - name: Publish diagnostics
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('node:fs');
            const report = fs.readFileSync('worker/typecheck.txt', 'utf8').slice(-30000);
            const exitCode = fs.readFileSync('worker/typecheck-exit.txt', 'utf8').trim();
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: 25,
              body: `## Unified platform TypeScript diagnostics\n\nExit: \`${exitCode}\`\n\n\`\`\`text\n${report}\n\`\`\``
            });
